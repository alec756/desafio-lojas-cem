generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Product {
  id             String   @id @default(uuid())
  name           String
  description    String?
  price          Decimal
  stock_quantity Int
  created_at     DateTime @default(now())

  @@map("products")
}
